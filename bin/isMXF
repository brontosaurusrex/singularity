#!/bin/bash

# help
if [ $# -eq 0 ]; then echo "Returns true if file seems to be MXF, to test: isMXF file.mov && echo yes"; exit 1; fi

# checks
command -v mediainfo >/dev/null 2>&1 || { >&2 echo "I need mediainfo installed." ; exit 1; }

# main
mediainfo "$1" | grep -q "MXF"



